//! Split legacy single-file `project.json` into geometry + render-settings files.
//!
//! Per port plan §8 Q7. Implemented later.

fn main() -> anyhow::Result<()> {
    anyhow::bail!("project-migrate not yet implemented");
}
